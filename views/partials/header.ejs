<header>
  <nav>
    <a href="/">Inicio</a>
    <% if (typeof user !== 'undefined' && user) { %>
      <a href="/logout">Salir</a>
    <% } else { %>
      <a href="/login">Iniciar Sesión</a>
    <% } %>
  </nav>
</header>
