<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/editBranch.css">
  <link rel="stylesheet" href="/css/header-footer.css">
</head>
<body>
  <%- include('../partials/header') %>

  <main>
    <h1><%= title %></h1>
    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>
    <% if (branch) { %>
      <form action="/empresa/branches/<%= branch.id %>" method="POST">
        <div class="form-group">
          <label for="name">Nombre de la Sucursal:</label>
          <input type="text" id="name" name="name" value="<%= branch.name %>" required>
        </div>
        <div class="form-group">
          <label for="location">Ubicación:</label>
          <input type="text" id="location" name="location" value="<%= branch.location %>" required>
        </div>
        <button type="submit">Actualizar Sucursal</button>
      </form>
    <% } else { %>
      <p>Sucursal no encontrada.</p>
    <% } %>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
